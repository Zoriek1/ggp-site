SANITY_STUDIO_PROJECT_ID=your_project_id_here
SANITY_STUDIO_DATASET=production

# Token usado APENAS pelo script de seed (npm run seed).
# Gere em https://sanity.io/manage > projeto > API > Tokens, role: Editor.
# NUNCA commitar este token.
SANITY_WRITE_TOKEN=
